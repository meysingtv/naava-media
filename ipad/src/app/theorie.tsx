import { Screen, ScreenHeader, CenterInfo } from "@/components/ui";

export default function TheorieScreen() {
  return (
    <Screen>
      <ScreenHeader title="Theorie" subtitle="Theorieunterricht & Teilnahme" />
      <CenterInfo text="Die Theorie-Verwaltung wird als Nächstes für das iPad gebaut." />
    </Screen>
  );
}
